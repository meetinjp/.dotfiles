[user]
	name = ${GIT_NAME}
	email = ${GIT_EMAIL}
	# signingkey is set by setup.sh after GPG key generation.

[commit]
	gpgsign = true

[tag]
	gpgSign = true

# Personal identity is the default everywhere (work-email auto-switch removed).
